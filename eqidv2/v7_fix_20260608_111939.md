# V7 Live System - Validated Fix Plan

Generated: 2026-06-08 11:19:39 IST  
Revalidated and corrected: 2026-06-08 IST  
Code changes applied: 2026-06-08 IST  
Audit basis: source code, BAT configuration, tests, runtime files, logs, and Windows scheduled tasks  
Status: **NOT live-safe. Keep `EQIDV2_live_trade_id_5min_v7_0900` disabled.**

### Applied 2026-06-08 — Code changes committed to working tree

| File | Change |
|---|---|
| `bat/run_eqidv2_signal_discovery_v7_replay_today.bat:13` | `ENTRY_LAG_MIN` 5 → 1 |
| `eqidv2_v7_signal_contract.py:9` | Docstring corrected — was `75 s`, now `30 s` |
| `eqidv2_v7_signal_contract.py:22` | `MAX_LAG_SEC` 15.0 → 30.0 |
| `bat/run_avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.bat:16` | `LATE_DETECTION_MAX_LAG_SEC` 75 → 30 |
| `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py:312` | Default `LATE_DETECTION_MAX_LAG_SEC` 75 → 30 |
| `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py:428` | `_entry_retry_deadline` now reads row `deadline_ist` as authoritative ceiling |
| `avwap_5min_ID_v7_live_scan.py:72` | `ENTRY_SIGNAL_TO_ENTRY_LAG_MIN` default 5 → 1 |
| `eqidv2_signal_discovery_v7_5min_id_persistent.py:812` | `_RANKER_MEMORY_CACHE.clear()` added on day rollover |
| `avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.py:1195` | Duplicate `_today_ist_str` definition removed |
| `bat/run_avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.bat:13` | Comment added: STOP/TARGET pct are fallback controls, not dead variables |

Restart all V7 processes for BAT and Python changes to take effect.  
The live executor (`PAPER_TRADE_FALSE`) remains disabled and must not be started until P0 items A1–C, E, F, G, I, J are also complete.

---

## Required Timing Contract

This is the selected V7 execution contract:

```text
Signal candle:       10:00:00
Intended execution:  10:01:00
Final deadline:      10:01:30
Execution price:     live LTP/fill available between 10:01:00 and 10:01:30
```

Contract rules:

1. `intended_entry_ist = signal_time_ist + 1 minute`.
2. `deadline_ist = intended_entry_ist + 30 seconds`.
3. The signal must pass discovery, post-filters, entry checks, risk checks, and reach the executor by `deadline_ist`.
4. Paper and live must enforce the same deadline.
5. A signal detected, handed off, or executed after `deadline_ist` is rejected with an explicit reason.
6. Missing or unparseable timing fields fail closed. `lag=None` must never pass freshness validation.
7. The forming 10:01 one-minute candle is not required and must not be treated as a completed bar.
8. Do not substitute `signal_close` and label it as the execution price.
9. Actual execution price is the live LTP or broker fill captured during the allowed 30-second window.
10. No setup-specific exception may extend the final deadline beyond 30 seconds.

Required aligned values:

```text
ENTRY_LAG_MIN=1
ENTRY_DUE_GRACE_SEC=30
MAX_SIGNAL_HANDOFF_LAG_SEC=30
LATE_DETECTION_MAX_LAG_SEC=30
```

The configured scheduler grace may reserve a small processing allowance before
the hard deadline. For example, a 30-second configured grace with a 2-second
processing reserve produces a maximum candidate-wait budget of 28 seconds. The
hard row-level `deadline_ist` remains T+1:30.

Exact timing sources and current status:

| File | Setting | Current working tree | Required |
|---|---|---:|---:|
| `bat/run_eqidv2_entry_engine_1min_v5_id.bat` | `EQIDV2_ID5MIN_V7_MAX_ENTRY_TO_DETECTION_LAG_SEC` | 30 | 30 - already set |
| `bat/run_eqidv2_entry_engine_1min_v5_id.bat` | `EQIDV2_ENTRY_ENGINE_1MIN_V5_ID_DUE_GRACE_SEC` | 30 | 30 - already set |
| `bat/run_eqidv2_entry_engine_1min_v5_id.bat` | `EQIDV2_ENTRY_ENGINE_1MIN_V5_ID_PROCESS_RESERVE_SEC` | 2 | Keep explicit; effective wait must end before deadline |
| `bat/run_eqidv2_entry_engine_1min_v5_id.bat` | `EQIDV2_ENTRY_ENGINE_1MIN_V5_ID_CANDIDATE_WAIT_SEC` | 30 | 30 - already set |
| `eqidv2_v7_signal_contract.py` | `MAX_LAG_SEC` | 15.0 | 30.0 |
| `eqidv2_v7_signal_contract.py` | module docstring | says `MAX_LAG_SEC (75 s)` — wrong on two counts | say `MAX_LAG_SEC (30 s)` |
| `bat/run_avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.bat` | `EQIDV2_LATE_DETECTION_MAX_LAG_SEC` | 30 | 30 - already set |
| `avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.py` | default late lag | 30 | 30 - already set |
| `bat/run_avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.bat` | `EQIDV2_LATE_DETECTION_MAX_LAG_SEC` | 75 | 30 |
| `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py` | default late lag | 75 | 30 |
| `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py` | `_entry_retry_deadline` | ignores row `deadline_ist` | make row deadline authoritative |

The June 8 SKYGOLD row contains an 11:51:15 deadline because the process that
wrote it was still running with the earlier 15-second configuration. The
current entry-engine and paper BAT files were edited to 30 seconds afterward.
A process restart is required before BAT changes affect its environment.

The writer already rejects `lag_sec > max_lag_sec` before appending a row.
Therefore, a 16-second detection under a 15-second writer limit is rejected by
the writer; it is not normally appended with an already-expired deadline.

---

## Validation Summary

| ID | Priority | Verdict | Required action |
|---|---|---|---|
| A1 | P0 | Valid with revision | Require dedicated dashboard credentials; remove all hardcoded and SMTP-password fallbacks |
| A2 | P0 | Valid | Make dashboard authorization fail closed |
| A3 | P1 | Conditional | Remove browser token only after Basic/session authentication handles all API calls |
| B1 | P0 | Already present in current working tree | Test V7 ID kill switches end-to-end |
| B2 | P1 | Partially present | Discovery and entry are restartable; add paper only after recovery testing |
| C | P0 | Original proposal rejected | Use live LTP execution and a hard T+1:30 deadline; do not use `signal_close` fallback |
| E | P0 | Valid with redesign | Gate on the authoritative complete slot manifest, not timestamp/state alone |
| F | P0 | Valid with redesign | Fully isolate replay through an explicit output context and unique run directory |
| G | P0 | Valid with additions | Atomic/versioned handoff snapshots plus locking for append-only files |
| G2 | P1 | Valid schema gap | Define and populate `stage2_detected_at_ist`; keep it for CSV compatibility |
| H1 | P0 | Already applied | Keep discovery BAT entry lag at one minute |
| H2 | P0 | Already applied | Keep discovery Python default at one minute |
| H3 | P2 | **Applied 2026-06-08** | `avwap_5min_ID_v7_live_scan.py:72` default changed 5 → 1 |
| H4 | P1 | **Applied 2026-06-08** | `bat/run_eqidv2_signal_discovery_v7_replay_today.bat:13` `ENTRY_LAG_MIN` changed 5 → 1 |
| P3-1a | P0 | **Applied 2026-06-08** | `eqidv2_v7_signal_contract.py:9,22` — docstring fixed (was 75 s); `MAX_LAG_SEC` 15.0 → 30.0 |
| P3-1b | P0 | **Applied 2026-06-08** | `bat/run_avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.bat:16` — `LATE_DETECTION_MAX_LAG_SEC` 75 → 30 |
| P3-1c | P0 | **Applied 2026-06-08** | `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py:312,428` — default 75 → 30; `_entry_retry_deadline` now reads row `deadline_ist` first |
| K | P2 | **Applied 2026-06-08** | `eqidv2_signal_discovery_v7_5min_id_persistent.py:812` — `_RANKER_MEMORY_CACHE.clear()` added on day rollover |
| L | P2 | **Applied 2026-06-08** | `avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.py:1195` — duplicate `_today_ist_str` removed |
| M | P2 | **Applied 2026-06-08** | `bat/run_avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.bat:13` — comment block added explaining fallback role |
| I | P0 | Original comment-only proposal insufficient | Enforce paper/live timing and safety parity in code/config |
| J | P0 | Original `mtime` proposal insufficient | Reconcile universes and validate each ticker's last candle against the slot |
| D1 | Rejected | Unsupported | Keep 30-minute lookback until benchmarks and feature-parity tests justify a change |
| D2 | P2 | Conditional | Reuse parquet only with strict causal completeness validation |
| N | Rejected | Unsafe migration | Preserve current signal IDs; design a versioned migration before changing algorithms |

---

## P0 - Must Fix Before Real Orders

### A1 - Dashboard Authentication Configuration

**Files**

- `bat/run_log_dashboard_server.bat`
- `log_dashboard_server.py`

Required behavior:

```bat
if "%LOG_DASH_REQUIRE_BASIC%"=="" set "LOG_DASH_REQUIRE_BASIC=1"

if "%LOG_DASH_USER%"=="" (
  echo [ERROR] LOG_DASH_USER is required.
  endlocal & exit /b 2
)

if "%LOG_DASH_PASS%"=="" (
  echo [ERROR] LOG_DASH_PASS is required.
  endlocal & exit /b 2
)
```

Requirements:

- Remove the `eqidv2` fallback.
- Do not reuse `LOG_DASH_SMTP_APP_PASSWORD`.
- Use a dedicated dashboard password or a strong dedicated API token.
- Validate credentials again in Python so direct Python startup also fails closed.
- Bind only to the intended interface and keep firewall access restricted.

### A2 - Dashboard Authorization Must Fail Closed

**File:** `log_dashboard_server.py`

Replace permissive behavior:

```python
if not username or not password:
    return True
```

with rejection:

```python
if not username or not password:
    return False
```

Also reject incomplete Basic configuration where only one of username/password is present.

### A3 - Remove Token Exposure After Authentication Migration

Do not expose the API token in:

- Served HTML.
- JavaScript constants.
- Query strings.
- Logs.

This must be deployed only after same-origin Basic authentication or a secure session mechanism covers dashboard API calls. Removing the token first would break kill/restart/status requests.

### B1 - V7 ID Kill Switches

The current working tree already contains:

- V7 ID paper/live kill-file constants.
- `_kill_switch_scope_paths` mappings.
- Whitelist entries.
- Snapshot handling.

Do not rewrite this logic. Verify:

1. `false_id_5min_v7` creates/removes the real-order kill file.
2. `true_id_5min_v7` creates/removes the paper kill file.
3. API returns HTTP 200 only after the requested file state is confirmed.
4. Executor sees the changed state.
5. Existing open-position handling follows the documented kill policy.
6. Unauthorized requests receive HTTP 401/403.

### B2 - Restart Controls

Discovery and entry-engine cards are already restartable in the current working tree.

The paper executor may be added only after testing:

- State recovery after process termination.
- No duplicate entry after restart.
- Open-position recovery.
- Idempotent daily CSV reload.
- Kill switch remains effective during restart.

The dashboard restart endpoint force-terminates a process tree. It must not be exposed before A1-A3.

### C - T+1 Live-LTP Execution Architecture

**Primary files**

- `eqidv2_signal_discovery_v7_5min_id_persistent.py`
- `eqidv2_entry_engine_1min_v5_id.py`
- `eqidv2_live_signal_writer.py`
- `avwap_trade_execution_PAPER_TRADE_TRUE_id_5min_v7.py`
- `avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py`
- corresponding BAT files

#### C1 - Remove the forming-bar requirement

The entry engine currently requires `_entry_bar_for_candidate(...)` at the intended T+1 timestamp. Current-minute availability from historical-data APIs is not reliable: June 8 SKYGOLD returned a forming T+1 bar, while other runtime fetches exposed only completed bars.

Do not replace the missing bar with `signal_close`.

Separate feature-bar eligibility from entry-reference eligibility.

**Momentum and indicator bars**

- Use only completed, causal bars.
- Require normalized `bar_end_ist <= intended_entry_ist`.
- Never use the high, low, close, volume or indicators of the forming
  intended-entry minute.
- Preserve the completed one-minute interval between signal close T and
  intended execution T+1 when it is part of the existing pre-entry momentum
  design. Do not move the cutoff back to T without replay, because that changes
  the filter.
- Normalize provider timestamps into explicit `bar_start_ist` and `bar_end_ist`.
  The 5-minute signal files are treated as end-labelled while Kite minute
  candles may be start-labelled; raw `date` comparisons are otherwise
  ambiguous.

**Entry reference**

- The actual execution price is live LTP/broker fill.
- If Kite returns the intended-entry minute while it is forming, only its
  immutable opening price may be used as an optional strategy reference.
- Do not use that forming bar's high, low, close, volume or indicators.
- Record the source as `forming_t1_open_reference`.
- If the intended-entry bar is absent, do not drop the candidate. Continue to
  the executor with live LTP and a reference source that the setup's tested
  risk-geometry policy supports.
- Never describe a forming-bar open as a completed candle or as the actual fill.

June 8 SKYGOLD evidence:

```text
signal_price:       490.75
T+1 open reference: 496.50
paper LTP fill:     496.35
writer time:        11:51:04
```

This proves that signal price, entry reference and actual fill are separate
price anchors and must not share one field name or semantic meaning.

#### C2 - Preserve existing setup risk geometry

Do not redesign setup stop/target rules merely to support live LTP.

For each accepted candidate:

1. Run existing setup-specific stop/target logic using its existing reference inputs.
2. Derive the stop and target distances from that reference.
3. At execution, rebase those same distances around the actual LTP/fill.
4. Recompute quantity using actual fill-to-stop risk.
5. Apply existing capital, quantity, margin, position and concurrency caps.

Persist:

```text
reference_price
reference_price_source
planned_stop_distance
planned_target_distance
planned_stop_distance_pct
planned_target_distance_pct
fill_price
fill_price_source
rebased_stop_price
rebased_target_price
quantity_before_rebase
quantity_after_rebase
```

Any setup whose stop/target cannot be safely represented or rebased must remain paper-only until replay proves parity.

#### C3 - Deadline enforcement

Every handoff row must contain:

```text
signal_time_ist
intended_entry_ist
deadline_ist
detected_time_ist
entry_engine_time_ist
executor_received_time_ist
execution_time_ist
```

At every stage:

```python
if now_ist > deadline_ist:
    reject("deadline_exceeded")
```

The real executor must not create its own 75-second or 300-second extension. `deadline_ist` is authoritative.

#### C4 - Overlap discovery and entry waiting

Runtime evidence showed an 11:35 candidate being created at about 11:36:17 after the entry engine had already stopped waiting around 11:36:10.

Additional June 8 evidence for slot 12:05:

```text
feed ready:           approximately T+55s
main V7 scan:         29.7s
Tier123 branch:       51.0s
discovery completion: T+107s (12:06:47)
hard deadline:        T+90s  (12:06:30)
```

The fixed `POST_SLOT_DELAY_SEC=75` BAT value is bypassed while the feed gate is
enabled, so it did not cause this 12:05 delay. It is still an unsafe fallback:
if the feed gate is disabled, a fixed 75-second sleep makes T+1:30 effectively
unreachable. Remove the fixed fallback or set it to zero and require the
authoritative feed gate in production.

The real bottleneck is cumulative feed-ready plus scan latency. With feed ready
at T+55, only 35 seconds remain for discovery, entry checks, writing, executor
receipt, quote and execution. Optional branches must not hold the production
candidate handoff past the deadline.

Required flow:

```text
completed 5m slot
  -> authoritative feed-ready marker
  -> discovery starts immediately
  -> filters run as candidates become available
  -> slot-specific candidate artifact/event is published atomically
  -> entry engine consumes it immediately
  -> executor waits for/reads live LTP
  -> execute only during T+1:00 through T+1:30
```

Avoid duplicate full-universe reads between scanner stages where the same data can be passed in memory or through a slot-specific manifest.

Required latency behavior:

- Publish production-core candidates as soon as their mandatory filters finish.
- Run optional Tier123/shadow branches independently; do not wait for them
  before publishing production candidates.
- Enforce a branch deadline based on remaining time, not only its start time.
- A branch started before its threshold must still be cancelled/ignored if it
  cannot publish before the slot deadline.
- Record feed-ready, scan-start, mandatory-scan-complete, handoff and executor
  timestamps separately.

### E - Authoritative Feed Gate

Do not treat a matching `slot_ist` or a non-blacklisted `overall_state` as sufficient.

Use the authoritative `slot_ready_5m/slot_*.json` marker and require:

```text
slot_ist == requested slot
complete == true
overall_state in an explicitly allowed set
tickers_expected > 0
tickers_written == tickers_expected
partition_failure_count == 0
verification_failed_count == 0
```

Unknown, missing, blank or malformed fields fail closed.

On timeout:

- Reject the slot.
- Write a structured rejection reason.
- Do not scan partial/stale parquet.
- Do not silently continue with the previous slot.

### F - Replay Isolation

Replay currently shares production path helpers. Replace ad-hoc path redirection with an explicit context:

```python
@dataclass(frozen=True)
class OutputPaths:
    mode: Literal["live", "replay"]
    root: Path
    daily_dir: Path
    latest_dir: Path
    audit_dir: Path
    status_dir: Path | None
```

Each replay must use:

```text
replay_shadow/<run_id>/
```

Redirect all replay writes:

- Raw and filtered daily CSVs.
- Slot JSON/CSV.
- Latest JSON/CSV.
- Audit files.
- Replay summaries.
- Diagnostics.

Replay must never touch:

- Production `latest_*` files.
- Production heartbeat.
- Production status.
- Production kill files.
- Production executor input.

Add a test that hashes production outputs before and after replay and verifies no change.

### G - Atomic and Versioned Handoff

Use atomic replacement for complete snapshot files:

1. Create a unique temporary file containing PID/thread or UUID.
2. Write complete content.
3. Flush and `fsync`.
4. Replace the target on the same volume with `os.replace`.
5. Retry transient Windows `PermissionError`.
6. Remove abandoned temporary files.

Add these fields to both JSON and CSV snapshots:

```text
snapshot_id
slot_ist
created_time_ist
candidate_count
schema_version
content_hash
```

The entry engine must reject mismatched JSON/CSV snapshot IDs and prefer the slot-specific JSON manifest. Daily append CSV files require a file lock and idempotent append key; they must not use whole-file replacement.

#### G2 - Define `stage2_detected_at_ist`

`stage2_detected_at_ist` exists in the CSV schema but the V7 writer currently
hardcodes it to an empty string. No active V7 paper or live executor currently
reads this field, so the empty value is not presently driving executor fallback
behavior. It is still an unresolved schema ambiguity.

Keep the column for compatibility and define it as:

```text
stage2_detected_at_ist =
    wall-clock time when the entry engine finishes mandatory post-filters
    and approves the candidate for signal writing
```

Implementation requirements:

- Capture it in the entry engine immediately after mandatory entry/post-filter
  acceptance.
- Pass it through the entry row.
- Make the signal writer copy the supplied value instead of hardcoding `""`.
- Keep `detected_time_ist` as the later CSV-write timestamp.
- Do not populate both fields from the same writer timestamp merely to make the
  column non-empty.
- Executors must use `deadline_ist` as the eligibility authority, not derive a
  new deadline from `stage2_detected_at_ist`.
- Add `stage2_to_write_lag_sec` and validate that Stage 2 cannot be after
  `detected_time_ist`.

### H - Entry-Lag Consistency

Already correct:

- Discovery BAT: `ENTRY_LAG_MIN=1`.
- Discovery Python default: `ENTRY_LAG_MIN=1`.
- Entry-engine BAT/Python: `ENTRY_LAG_MIN=1`.
- Signal contract/writer: one-minute intended entry.

Still required:

- Change `avwap_5min_ID_v7_live_scan.py` legacy default from five to one.
- Label the retired combined scanner clearly so it cannot be mistaken for the active path.

**H4 - replay configuration**

File: `bat/run_eqidv2_signal_discovery_v7_replay_today.bat`

```bat
-set EQIDV2_SIGNAL_DISCOVERY_V7_ENTRY_LAG_MIN=5
+set EQIDV2_SIGNAL_DISCOVERY_V7_ENTRY_LAG_MIN=1
```

Replay and live must use the same intended-entry contract unless a replay is
explicitly named and isolated as a different timing experiment.

### I - Paper and Live Safety Parity

Comments alone are not a fix.

Required before real orders:

- Paper and live both use the 30-second deadline.
- Live honors row-level `deadline_ist`.
- Remove live's 75-second global allowance.
- Remove the executor's independent 300-second retry window.
- Missing detection lag/timestamps fail closed.
- Remove `EQIDV2_CONFIG_CHECK_BYPASS=1`; configuration mismatch must stop live startup.
- Decide quantity policy explicitly. Do not silently compare paper signal quantity with live forced quantity one.
- Apply equivalent maximum-position, maximum-capital and daily-loss controls.
- Preserve paper-only controls only when clearly marked as simulation aids.

Correct executor documentation:

- Paper backfills missing stop/target values from configured percentages.
- Live currently rejects missing stop/target values rather than backfilling them.
- The configured percentages are fallback controls in paper, not informational/dead variables.

### J - Universe and Per-Ticker Freshness

The discovery universe contained 1,280 symbols while the feed wrote 1,253. Twenty-seven discovery symbols overlapped the invalid/quarantine list.

Do not use file modification time as the freshness decision.

Required:

1. Publish the exact expected ticker universe in the feed slot manifest.
2. Reconcile feed and discovery universes at startup and each session.
3. Validate each ticker's last candle timestamp against the requested slot.
4. Never scan an old candle as though it belonged to the current slot.
5. Report missing, quarantined, invalid and stale symbols separately.
6. Do not silently skip a ticker. A missing expected ticker must produce a visible slot-health failure or an explicitly approved quarantine reason.
7. Do not allow candidates from tickers absent from the authoritative feed-complete set.

---

## P1 - Required Reliability Tests

Add pipeline-specific tests for:

1. Signal `10:00`, intended entry `10:01`, deadline `10:01:30`.
2. Arrival at `10:01:29` is eligible.
3. Arrival after `10:01:30` is rejected.
4. Missing T+1 forming candle does not block live-LTP execution.
5. No future/incomplete one-minute bar enters momentum features.
6. Missing `deadline_ist`, detection time or lag fails closed.
7. Partial feed manifest rejects the slot.
8. Partial/malformed latest JSON cannot fall back to stale data.
9. Restart does not duplicate a signal/order.
10. Replayed slots cannot alter production outputs.
11. Paper and live produce identical eligibility decisions.
12. Existing signal IDs remain deduplicated.
13. Kill and restart endpoints require authentication.
14. `stage2_detected_at_ist` is populated before `detected_time_ist` and is never used to extend the deadline.
15. Signal, reference, quote and fill prices remain separately attributable.

Boundary behavior at exactly `10:01:30` must be chosen once and tested consistently. Recommended: allow `execution_time_ist <= deadline_ist`.

---

## P2 - Safe Cleanup and Conditional Optimizations

### D1 - Keep the 30-Minute Kite Lookback

Reject the proposed reduction to 20 minutes for now.

Measured Kite raw fetch time was approximately 0.13-0.68 seconds, while discovery and handoff latency dominated. The momentum calculations also use prior-volume context that may benefit from more than 20 bars.

Change only after:

- Timing benchmark proves meaningful savings.
- Feature values are byte-for-byte or tolerance-equivalent.
- Candidate pass/fail parity is preserved.

### D2 - Conditional Indicator-Parquet Reuse

Parquet reuse is allowed only when:

- Required columns exist.
- Timestamps are timezone-normalized.
- No row is later than the causal cutoff.
- The latest completed expected one-minute bar exists.
- The file belongs to the same trading day.
- No gaps affect the required momentum window.

The old `slot - 3 minutes` freshness test is rejected as too loose.

Fallback to Kite only when the cache fails validation. Log cache/fetch source and validation reason.

### K - Clear Ranker Cache on Day Change

Clear `_RANKER_MEMORY_CACHE` together with the universe cache during day rollover. This is mainly for replay and long-running test processes.

### L - Remove Duplicate `_today_ist_str`

Keep one definition placed before its callers. Remove the identical duplicate without changing behavior.

### M - Correct Stop/Target Configuration Comments

Use:

```bat
rem STOP/TARGET percentages are fallback controls in paper mode.
rem Existing setup-specific values from the signal row remain primary.
rem Do not describe these variables as informational or dead.
```

### N - Preserve Existing Signal IDs

Do not switch directly to `contract.canonical_signal_id`.

Validation showed that it differs from the current `_generate_signal_id` for sampled and June 5 signals. A direct switch can bypass deduplication and create duplicate execution.

If migration is required:

1. Introduce `signal_id_version`.
2. Continue reading/deduplicating legacy IDs.
3. Emit both legacy and new IDs in shadow mode.
4. Prove deterministic parity or define a controlled cutover date.
5. Test restart and cross-midnight behavior.

Until then, the legacy generator remains the production contract.

---

## Strategy Decisions

### LONG Signals

`SHORT_FOCUS=1` is an explicit strategy setting, not a pipeline bug.

Do not enable LONG merely as part of the safety repair. Evaluate LONG separately through:

- Honest replay.
- Holdout results.
- Combined-side capital/exposure limits.
- Setup-specific filter validation.
- Paper soak after all P0 timing and feed fixes.

Retired scanner limits such as `MAX_TRADES_PER_DAY` and `MAX_SAME_SIDE_PER_DAY` must not be treated as active entry-engine controls unless they are explicitly wired into the active path.

### Daily Loss Brake

Do not permanently select Rs 10,000 without calibration.

Replay:

```text
Rs 7,500
Rs 10,000
Rs 15,000
fractional equity/risk alternatives
```

Choose using drawdown reduction, missed recovery trades, profit factor, expected value and tail-loss behavior. Keep the real executor disabled until a brake is enabled and tested.

---

## Implementation Order

### Phase 1 - Security and Operator Controls

1. A1/A2: fail-closed dashboard authentication.
2. A3: remove token exposure after UI auth migration.
3. B1: test existing V7 kill scopes.
4. B2: test restart recovery; add paper restart only if idempotent.

### Phase 2 - Data Integrity

1. E: authoritative slot-ready manifest.
2. J: universe reconciliation and per-ticker candle freshness.
3. G: atomic/versioned snapshots and append locking.
4. F: complete replay isolation.

### Phase 3 - T+1:30 Execution Contract

1. Align all lag/deadline values to one minute plus 30 seconds.
2. Remove forming-candle dependency.
3. Add live-LTP/fill execution path.
4. Rebase existing stop/target distances without changing setup rules.
5. Recompute quantity from actual fill risk.
6. Make `deadline_ist` authoritative in paper and live.
7. Remove live timing extensions and config bypass.

#### Phase 3 Step 1 — Exact diffs for remaining open timing items

Three files still require changes. Entry-engine and paper-executor bats are
already at 30 seconds (confirmed 2026-06-08). The following are not yet applied.

**`eqidv2_v7_signal_contract.py` — two changes in the same file**

```python
# Line 7 (module docstring) — fix wrong number:
-  detection_lag_sec >= 0 and <= MAX_LAG_SEC (75 s)
+  detection_lag_sec >= 0 and <= MAX_LAG_SEC (30 s)

# Line 22 (constant):
-MAX_LAG_SEC: float = 15.0
+MAX_LAG_SEC: float = 30.0
```

Note: the docstring said 75 s while the constant was 15.0. Both were wrong.
The constant controls the signal writer's pre-write gate and all contract
validation calls that use the default. Changing it to 30.0 aligns the Python
module with the BAT-level configuration already applied.

**`bat/run_avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.bat` — line 16**

```bat
-set "EQIDV2_LATE_DETECTION_MAX_LAG_SEC=75"
+set "EQIDV2_LATE_DETECTION_MAX_LAG_SEC=30"
```

**`avwap_trade_execution_PAPER_TRADE_FALSE_id_5min_v7.py` — two changes**

```python
# Python default (line 312):
-LATE_DETECTION_MAX_LAG_SEC = int(os.getenv("EQIDV2_LATE_DETECTION_MAX_LAG_SEC", "75"))
+LATE_DETECTION_MAX_LAG_SEC = int(os.getenv("EQIDV2_LATE_DETECTION_MAX_LAG_SEC", "30"))
```

The `_entry_retry_deadline` function in this file also ignores the row-level
`deadline_ist` field and computes its own deadline from `detected_time_ist +
ENTRY_RETRY_WAIT_SEC` (300 seconds). This must be fixed as part of item 6
(make `deadline_ist` authoritative): parse `deadline_ist` from the signal row
first and use it as the ceiling, falling back to the computed value only for
legacy rows where the field is absent or empty.

### Phase 4 - Pipeline Tests

Implement the P1 tests, including timing boundaries, causal bars, partial feed, duplicate restart and replay isolation.

### Phase 5 - Paper Validation

Run paper mode only and report:

```text
signal-to-detection latency
detection-to-handoff latency
handoff-to-executor latency
execution latency
deadline rejection count
feed rejection count
duplicate suppression count
signal_price-to-entry_reference divergence pct
entry_reference-to-LTP quote divergence pct
LTP quote-to-broker fill slippage bps
entry_reference-to-actual_fill divergence pct
signal_price-to-actual_fill divergence pct
stop/target rebase distance
paper/live eligibility parity
```

Use explicit price fields:

```text
signal_price
entry_reference_price
entry_reference_source
execution_quote_ltp
actual_fill_price
```

Do not call all of these values `entry_price`. For paper mode,
`actual_fill_price` is the simulated LTP fill. For real orders, retain both the
pre-order quote and broker average fill.

Promotion criterion: the contract must be met without setup-specific freshness exceptions and without silently dropping or accepting stale candidates.

### Phase 6 - Risk Calibration

Replay daily-loss-brake alternatives and verify maximum positions, capital, quantity and same-ticker controls.

### Phase 7 - Real-Order Readiness

Enable real orders only after:

- All P0 items pass.
- Pipeline tests pass.
- Paper/live eligibility decisions match.
- Kill switch and restart recovery pass.
- No production file is modified by replay.
- Configuration attestation is enabled.
- Daily loss brake is enabled and calibrated.
- A controlled minimal-quantity live trial is explicitly approved.

---

## Final Verdict

The selected behavior is:

```text
Signal candle:       10:00
Intended execution:  10:01
Final deadline:      10:01:30
```

This cannot be made reliable by renaming timestamps, waiting for the forming 10:01 candle, or using `signal_close` as a fake execution price. The system needs an overlapping discovery/handoff path, authoritative feed readiness, live-LTP execution, and one hard deadline shared by entry, paper and live executors.

Until these changes are implemented and verified, V7 is suitable for paper observation only and is **not live-safe for real orders**.
